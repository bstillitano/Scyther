name: Build and Deploy Documentation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.0'

      - name: Build Documentation
        run: |
          # Build documentation using xcodebuild with iOS destination
          xcodebuild docbuild \
            -scheme Scyther \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            -derivedDataPath ./docbuild \
            OTHER_DOCC_FLAGS="--transform-for-static-hosting --hosting-base-path Scyther"

          # Find the generated .doccarchive
          DOCC_ARCHIVE=$(find ./docbuild -name "*.doccarchive" -type d | head -1)
          echo "Found archive: $DOCC_ARCHIVE"

          # Copy the documentation to the output directory
          mkdir -p ./docs
          cp -R "$DOCC_ARCHIVE"/* ./docs/

          # Add CNAME for custom domain
          echo "scyther.io" > ./docs/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
